# LoRaTool v9.7 運行 Bug 報告

## 概覽
經由對 `LoRaTool-v9.7.apk` 對應之原始碼 (`React-Native-Source/App.tsx`) 的深入分析，發現該版本存在多項運行時 Bug 及功能性缺失。雖然 UI 渲染正常，但核心功能（BLE 掃描、參數讀寫、持久化存儲）大部分為模擬數據（Mock），且存在會導致崩潰或操作無效的代碼錯誤。

---

## 1. 嚴重級別：高 (Critical) - 核心功能失效

### 1.1 掃描功能被硬編碼為模擬數據
- **問題**：`DevicesTab` 中的「掃描」按鈕目前調用的是 `mockScan()` 函數。
- **影響**：用戶點擊掃描後，看到的始終是硬編碼的 5 個模擬設備（Cloudtek-LW01 等），無法掃描到真實的 LoRa 設備。
- **原始碼位置**：`App.tsx` 第 720 行。

### 1.2 缺少 Android 運行時權限請求
- **問題**：App 雖然導入了 `PermissionsAndroid`，但未在啟動或掃描前請求 `BLUETOOTH_SCAN`、`BLUETOOTH_CONNECT` 與 `ACCESS_FINE_LOCATION`。
- **影響**：在 Android 12+ 設備上，即使切換回真實掃描模式，也會因為缺少權限而導致掃描失敗或 App 閃退。

### 1.3 核心模組缺失 (Buffer/Clipboard)
- **問題**：代碼中多處涉及十六進制轉換與數據處理，但未正確導入 `buffer` 庫；且 `Clipboard` 未導入。
- **影響**：在執行真實的數據發送 (`sendCommand`) 或嘗試複製日誌時，App 將拋出 `ReferenceError` 並崩潰。

---

## 2. 嚴重級別：中 (Medium) - UI 與邏輯錯誤

### 2.1 樣式名拼寫錯誤 (Style Typo)
- **問題**：`DevicesTab` 掃描按鈕文字使用了錯誤的樣式鍵 `S.pillTTActive`。
- **影響**：按鈕文字樣式失效，導致文字顏色可能與背景衝突，視覺外觀異常。
- **原始碼位置**：`App.tsx` 第 721 行。

### 2.2 地圖導航邏輯缺陷
- **問題**：在「地圖」分頁點擊設備卡片時，僅調用 `connectDevice(d)`，但未切換 `tab` 狀態。
- **影響**：用戶點擊後 UI 無反應（仍停留在地圖頁），不會自動跳轉到「設備」或「控制面板」，導致用戶誤以為點擊無效。

---

## 3. 嚴重級別：低 (Low) - 基礎建設不全

### 3.1 存儲與定位完全模擬
- **問題**：`AsyncStorage` 與 `Geolocation` 被替換成了純 JS 的 Mock 實現。
- **影響**：
  - 數據無法真正保存在手機本地，重啟 App 後設備列表會消失。
  - GPS 座標固定在台北（25.033, 121.565），無法獲取設備真實位置。

---

## 總結與建議
目前的 `v9.7` 版本更像是一個 **UI 演示版 (Demo)** 而非可投入生產的工具。
**建議修復步驟：**
1. 修正 `pillTTActive` 拼寫錯誤。
2. 導入 `Buffer` 與 `Clipboard`。
3. 實作啟動時的權限請求邏輯。
4. 將掃描按鈕重新接回 `startScan` 並加入權限校驗。
5. 修復地圖分頁的跳轉邏輯。
